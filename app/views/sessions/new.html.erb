<%= form_for(:session, url: login_path ) do |f| %>
	
	<%= f.label :roll_no %>
	<%= f.text_field	:roll_no, placeholder: "Roll no"  %>
	
	<%= f.label :password, placeholder: "Password"  %>
	<%= f.password_field :password %>

	<%= f.submit "Sign in" %>
<% end %>
